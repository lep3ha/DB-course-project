services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: blank_supplies_web:latest
    env_file: .env
    ports:
      - "5001:5000"
    volumes:
      - .:/app
    working_dir: /app
    command: gunicorn -w 4 -b 0.0.0.0:5000 run:app
    restart: unless-stopped
